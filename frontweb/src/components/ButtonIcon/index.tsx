import './styles.css';

const ButtonIcon = () => {
  return (
    <div className="btn-container">
      <button className="btn btn-icon">
        <p>VER CATÁLOGO</p>
      </button>
    </div>
  );
};

export default ButtonIcon;
